slug: api-client-origin-guard
version: 1
title: ""
phases:
    - number: 1
      slug: gui-origin-guard
      title: GUI API Client Origin Guard
      diff_base: 3225c4bd92c12cf5b122dd905ee4b20e3cfc51c3
      tasks:
        - number: 1
          slug: implement-origin-guard
          title: Implement Origin Guard
          path: phase-01-gui-origin-guard/001-implement-origin-guard.md
          done: true
          tier: sonnet-high
          digest: 'Added an opt-in allowedOrigins: string[] allow-list guard to gui/src/lib/api-client.ts, resolving followup 7VD3. configureApiClient() now accepts both auth-handler fields and allowedOrigins, merging each independently. New private resolveRequestOrigin() helper resolves target origin from string|URL input (browser-relative-safe, SSR-safe, never throws). request() enforces the guard as its first statement, throwing ApiRequestError(''origin_not_allowed'', ..., 0) before any network call when the origin is disallowed or unresolvable. Unconfigured default is a complete no-op, verified byte-for-byte against pre-change control flow. AGENTS.md''s api-types.ts bullet updated accordingly. tsc --noEmit passes with zero errors; no automated test runner exists in gui/ (followup vr20, out of scope).'
          git:
            branch: phase-01-task-01-implement-origin-guard
            commit:
                - 36c3c5d
            merge: e8b944e974ffd8c0a636fca829adb322ca37a6d0
