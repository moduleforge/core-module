# Phase 3, Task 3 — Delete 0000–0005 from users-module/model/migrations

## Context
Core migrations now live in core-module. users-module must stop shipping its own copy.

## Acceptance
- `users-module/model/migrations/0000_helpers.sql` through `0005_service_accounts.sql` deleted.
- `users-module/model/migrations/atlas.sum` deleted (will be regenerated against composed dir).

## How to verify
- `ls users-module/model/migrations/` shows only files starting with `0006_` or higher.
- The deletion is staged for commit.

## Notes
- Must be done AFTER task 3.2 so users-module still builds during the intermediate commit.
- `atlas.sum` is regenerated by Phase 3.10 on the composed dir.
